# Environment Configuration
NODE_ENV=production
PORT=3000

# Client URL for CORS (only needed in production)
CLIENT_URL=https://your-app-name.up.railway.app

# Optional: Database URL if you add persistence later
# DATABASE_URL=

# Optional: Redis URL for scaling across multiple servers
# REDIS_URL=
